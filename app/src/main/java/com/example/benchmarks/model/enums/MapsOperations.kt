package com.example.benchmarks.model.enums

enum class MapsOperations(val operationName: String) {
    ADDING_NEW("Adding new in"),
    SEARCH_BY_KEY("Search by key in"),
    REMOVING("Removing in")
}